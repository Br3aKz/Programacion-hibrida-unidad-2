import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.unidad2.maximiliano.quiroz',
  appName: 'evaluacion-unidad2-maximiliano-quiroz',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
